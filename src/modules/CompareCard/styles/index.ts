export const cardStyles = `
    position: relative;
    min-width: fit-content;
    
    margin-bottom: 20px;
    padding: 0 50px;

    border-left: 2px solid #FFFFFF;
`;

export const mainInfo = `
    margin-bottom: 61px;

    h3 {
        margin: 0;
        margin-bottom: 61px;

        text-align: left;
    }

    p {
        margin: 0;
        margin-bottom: 20px;

        text-align: left;
    }
`;

export const additionInfo = `
    p {
        margin: 0;
        margin-bottom: 15px;

        text-align: left;
    }
`;

export const crossStyles = `
    position: absolute;
    top: -5px;
    right: 15px;

    background: transparent;
    outline: none;
    border: none;

    cursor: pointer;
`;